interface:
  display_name: "Session Log Triage"
  short_description: "Cluster repeated failures and recurring user asks from session logs."
  default_prompt: "Scan recent session logs, normalize tool-failure lines, cluster top repeated issues, and include repeated user request patterns with excerpts."
