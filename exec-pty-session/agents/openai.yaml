interface:
  display_name: "Exec Command TTY Sessions"
  short_description: "通过 tty 会话避免 write_stdin 失败。"
  default_prompt: "Handle interactive exec_command sessions and write_stdin usage safely, including tty=true and session_id reuse."
