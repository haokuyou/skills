interface:
  display_name: "Git Default Repo"
  short_description: "Report the default remote repo and default branch for a project."
  default_prompt: "Find the default git remote and default branch for this project."
