interface:
  display_name: "PATH Update Warning Mitigation"
  short_description: "Handle PATH and timeout binary lookup failures."
  default_prompt: "Mitigate exec_command PATH lookup failures (including missing timeout/gtimeout) using absolute paths, inline PATH, and watchdog fallback."
