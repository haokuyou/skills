interface:
  display_name: "Version Bump Basic"
  short_description: "处理“将版本改成3”等请求，查找并更新版本字段。"
  default_prompt: "Handle version bump requests such as change version to X or 将版本改成3, then update the relevant project version fields."
