interface:
  display_name: "Sandbox Denied Recovery"
  short_description: "从 sandbox 拒绝或策略阻止的 exec_command 运行中恢复。"
  default_prompt: "Diagnose and recover from exec_command sandbox denied or blocked-by-policy errors with safe path and workdir adjustments."
