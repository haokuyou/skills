interface:
  display_name: "Sandbox Denied Recovery"
  short_description: "Recover from sandbox denied exec_command runs."
  default_prompt: "Diagnose and recover from exec_command sandbox denied errors with safe path and workdir adjustments."
