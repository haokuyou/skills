// Based on upstream: docs/uts/uts_diff_ts.md
// Use null instead of undefined in UTS.

export function greet(name: string | null): string {
  if (name == null) {
    return "Hello, stranger"
  }
  return `Hello, ${name}`
}

